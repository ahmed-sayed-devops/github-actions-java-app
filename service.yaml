apiVersion: v1
kind: Service
metadata:
  name: simple-java-app-service

spec:
  type: NodePort

  selector:
    app: simple-java-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007